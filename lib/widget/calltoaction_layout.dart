import 'package:flutter/material.dart';

class CallToActionLayout extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Expanded(
      child: Container(
        child: Center(
          child: Text('Call To Action Layout'),
        ),
      ),
    );
  }
}
